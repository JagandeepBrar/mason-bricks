name: service_api_type
description: Create a new enumerated type class for a service API package
version: 1.0.0+1

environment:
  mason: ">=0.1.0-dev.41 <0.1.0"

vars:
  service:
    type: string
    prompt: What is the service name?
  name:
    type: string
    prompt: What is the type name?

